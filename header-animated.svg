
<svg width="1000" height="250" viewBox="0 0 1000 250" xmlns="http://www.w3.org/2000/svg">
  <style>
    .title {
      font: bold 40px 'Courier New', monospace;
      fill: #f5025b;
      opacity: 0;
      animation: fadeIn 1s ease-in-out forwards;
    }

    .subtitle {
      font: 20px 'Courier New', monospace;
      fill: white;
      opacity: 0;
      animation: fadeIn 1s ease-in-out forwards;
    }

    .subtitle-1 {
      animation-delay: 2s;
    }

    .subtitle-2 {
      animation-delay: 3s;
    }

    .cursor {
      animation: blink 1s step-start infinite;
      fill: white;
    }

    .logos {
      opacity: 0;
      animation: fadeIn 1s ease-in-out forwards;
      animation-delay: 4s;
    }

    @keyframes fadeIn {
      to {
        opacity: 1;
      }
    }

    @keyframes blink {
      50% {
        opacity: 0;
      }
    }
  </style>

  <rect width="100%" height="100%" fill="#0d1117"/>
  <text x="500" y="110" text-anchor="middle" class="title">
    Matthieu De Langhe
  </text>
  <text x="500" y="160" text-anchor="middle" class="subtitle subtitle-1">
    smart contracts & embedded systems
  </text>
</svg>
